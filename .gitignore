/docs/
/memory-export/
PLAN.md
/.agents
/.cursorrules
/core-scratch/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Offline embedding model cache. Tacit falls back to a project-local cache when
# the per-user cache is not writable (sandboxes, containers) — that download is
# ~50MB and must never be committed.
.tacit/models/
.tacit/_*/